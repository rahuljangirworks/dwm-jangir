[Greeter]
default-session=dwm-jangir

# ── Background ───────────────────────────────────────────
# The wrapper chooses one bundled wallpaper before the greeter starts. Only
# that image is decoded for a login screen; the greeter never downloads data.
background=/run/lightdm/dwm-jangir-background
# Nord dark fallback shown while wallpaper loads
background-color=#2E3440
# Span wallpaper across all monitors instead of repeating
stretch-background-across-monitors=true
# Subtle dot-grid overlay for depth
draw-grid=true
# Don't replace wallpaper with per-user images
draw-user-backgrounds=false

# ── Logo ─────────────────────────────────────────────────
# The patched greeter supplies its own compact visual identity
logo=

# ── Login panel ──────────────────────────────────────────
# Centre the login box on screen
content-align=center
theme-name=dwm-jangir-dark
icon-theme-name=Adwaita

# Cantarell is shipped by Fedora and remains legible at every supported DPI.
font-name=Cantarell 11
xft-antialias=true
xft-hintstyle=hintslight
xft-rgba=rgb
xft-dpi=96

# Minimal panel: hostname on the left and one power menu on the right. The
# large clock is rendered above the login card.
show-clock=false
clock-format=%I:%M %p
show-hostname=true
show-power=false
show-a11y=false
show-keyboard=false
show-quit=true

enable-hidpi=auto
activate-numlock=false
